CORS_ALLOWED_ORIGINS=""
# addresses must be separated by commas example: "http://localhost:3000,http://myapp.com"
NATS_URL="nats://nats:4222"
# JWT secret — must match SECRET_API_KEY used by the controller service
SECRET_API_KEY=""
# Comma-separated email addresses that receive all device notifications (admin role)
ADMIN_EMAILS=""
# Optional: webhook endpoint for mobile/miniprogram push (Phase 2)
WEBHOOK_URL=""
